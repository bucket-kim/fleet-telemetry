import styled from "styled-components";

export const NotificationStyleContainer = styled.div`
    position: fixed;
    top: 1rem;
    right: 1rem;
    z-index: 10;
    display: flex;
    flex-direction: column;
    gap: 1rem;
`